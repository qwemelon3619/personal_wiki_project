# Local Azurite connection string for development
# Use this file to set environment variables when running docker-compose locally.
# Azurite default account/key and endpoint
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFeq...;BlobEndpoint=http://azurite:10000/devstoreaccount1;
AZURE_STORAGE_CONTAINER_NAME=photos

# Example: to use this file with docker-compose, run:
#   export $(cat .env.development | xargs) && docker-compose up
